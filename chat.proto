syntax = "proto3";

package chat;
option go_package = "./;chat";

message Message {
    string planeta = 1;
    string ciudad = 2;
    string valor = 3;
}

service Chat {
    rpc SendMessage(Message) returns (Message) {}
}